/**
 * Standalone StatusBadge component — re-exports from EligibilityCard
 * so it can be imported alone: import StatusBadge from './StatusBadge'
 */
export { StatusBadge as default } from "./EligibilityCard";
